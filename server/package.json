{
  "name": "server",
  "version": "1.0.0",
  "description": "Mai Tu build ticket database with mongodb",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "dev": "nodemon server.js",
    "start": "node server.js",
    "test": "test",
    "build": "npm install && npm install -g nodemon && npm install -g bcrypt && npm install -g body-parser && npm install -g cloudinary && npm install -g cookie-parser && npm install -g cors && npm install -g dotenv && npm install -g express && npm install -g http && npm install -g http-errors && npm install -g jsonwebtoken && npm install -g lodash && npm install -g moment && npm install -g mongoose && npm install -g multer && npm install -g node-cron && npm install -g nodemon && npm install -g socket.io"
  },
  "keywords": [
    "TS"
  ],
  "author": "Mai Tu",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^6.0.0",
    "body-parser": "^2.2.0",
    "cloudinary": "^2.6.1",
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "http": "^0.0.1-security",
    "http-errors": "^2.0.0",
    "jsonwebtoken": "^9.0.2",
    "lodash": "^4.17.21",
    "moment": "^2.30.1",
    "mongoose": "^8.15.2",
    "multer": "^2.0.1",
    "node-cron": "^4.1.0",
    "nodemon": "^3.1.10",
    "socket.io": "^4.8.1"
  }
}
